# Custom Addons (Local Experiments Only)

Production custom modules live in the separate
[grove-odoo-modules](https://github.com/Goldberry-Playground/grove-odoo-modules) repo
and are deployed via git-sync to `/workspace/current`.

This directory is for **local-only experimental modules** during development.
Anything here is bind-mounted into the container but is NOT deployed to production.
